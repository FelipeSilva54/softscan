diff --git a/node_modules/expo-camera/android/src/main/java/expo/modules/camera/ExpoCameraView.kt b/node_modules/expo-camera/android/src/main/java/expo/modules/camera/ExpoCameraView.kt
index d9690e3..2a412b4 100644
--- a/node_modules/expo-camera/android/src/main/java/expo/modules/camera/ExpoCameraView.kt
+++ b/node_modules/expo-camera/android/src/main/java/expo/modules/camera/ExpoCameraView.kt
@@ -531,7 +531,16 @@ class ExpoCameraView(
     ImageAnalysis.Builder()
       .setResolutionSelector(
         ResolutionSelector.Builder()
-          .setResolutionStrategy(ResolutionStrategy.HIGHEST_AVAILABLE_STRATEGY)
+          // HIGHEST_AVAILABLE_STRATEGY faz o ML Kit analisar cada frame na maior
+          // resolução que a câmera oferece (ex: 4000x3000+), o que pode levar
+          // segundos por frame em aparelhos médios/entrada — daí a demora percebida
+          // ao escanear. 1280x720 é a resolução que o próprio Google recomenda nos
+          // exemplos de ML Kit/CameraX para leitura de código de barras em tempo
+          // real: suficiente para decodificar QR e barras 1D, mas ordens de
+          // magnitude mais rápida de processar.
+          .setResolutionStrategy(
+            ResolutionStrategy(Size(1280, 720), ResolutionStrategy.FALLBACK_RULE_CLOSEST_LOWER_THEN_HIGHER)
+          )
           .build()
       )
       .setBackpressureStrategy(ImageAnalysis.STRATEGY_KEEP_ONLY_LATEST)
